Add distinct, polished hover interactions for TextLink components. The site uses a highlight-style link (colored background wash behind link text) as the current default. Each variant should implement one hover approach in src/components/TextLink.tsx and src/styles/globals.css, wired through the existing toggletation panel (linkHoverStyle field). Hover should feel intentional and match the site's quiet, considered aesthetic — subtle motion, no flashy effects.
